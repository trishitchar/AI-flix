import React from 'react'

const fetchToken = () => {
    useEffect(() => {
        const token = getCookie('token');
        console.log(token);
        
        
      }, []);
    
    return (
    {}
  )
}

export default fetchToken;